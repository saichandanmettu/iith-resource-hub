# Local-only: superseded versions, UI experiments, unused source images.
# Nothing in here is deployed.
_local/

# OS / editor
.DS_Store
*.log
node_modules/
.claude/

# backend — never commit secrets or user uploads
api/config.php
*.lock
.tmp-*
decisions.json
