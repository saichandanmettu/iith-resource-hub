# Approved PDFs are static files. Nothing here should ever execute.
# Belt and braces: even a bad file that reached this folder cannot run.
<FilesMatch "\.(php|phtml|php[0-9]|phar|cgi|pl|py|sh)$">
  Require all denied
</FilesMatch>
Options -Indexes -ExecCGI
AddType text/plain .php .phtml .phar
